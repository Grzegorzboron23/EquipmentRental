spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: Plmoknij1.
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:liquibase-changelog.xml
page:
  size: 10
